/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "uploads-ssl.webflow.com",
      "picsum.photos",
      "assets.website-files.com",
      "randomuser.me",
    ],
  },
};

module.exports = nextConfig;
